# Migration `20190818093035`

This migration has been generated by Tyler Maschino at 8/18/2019, 9:30:35 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_User"("id" TEXT NOT NULL  ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"email" TEXT NOT NULL DEFAULT '' ,"password" TEXT NOT NULL DEFAULT '' ,"resetToken" TEXT   ,"resetTokenExpiry" INTEGER   ,"subscriptionLevel" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

INSERT INTO "new_User" ("id","createdAt","updatedAt","email","password","resetToken","resetTokenExpiry") SELECT "id","createdAt","updatedAt","email","password","resetToken","resetTokenExpiry" from "User"

DROP TABLE "lift"."User";

ALTER TABLE "lift"."new_User" RENAME TO "User";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190817083905..20190818093035
--- datamodel.dml
+++ datamodel.dml
@@ -8,16 +8,18 @@
   provider = "photonjs"
 }
 model User {
-  id               String     @default(cuid()) @id @unique
-  createdAt        DateTime   @default(now())
-  updatedAt        DateTime   @updatedAt
-  email            String     @unique
-  password         String
-  resetToken       String?
-  resetTokenExpiry Int?
-  sites            UserSite[]
+  id                String     @default(cuid()) @id @unique
+  createdAt         DateTime   @default(now())
+  updatedAt         DateTime   @updatedAt
+  email             String     @unique
+  password          String
+  resetToken        String?
+  resetTokenExpiry  Int?
+  sites             UserSite[]
+  subscriptionLevel String
+
 }
 model UserSite {
   id               String    @default(cuid()) @id @unique
```

## Photon Usage

You can use a specific Photon built for this migration (20190818093035)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190818093035'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
