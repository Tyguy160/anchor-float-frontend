# Migration `20190817081430`

This migration has been generated by Tyler Maschino at 8/17/2019, 8:14:30 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_UserSite"("id" TEXT NOT NULL  ,"createdAt" DATE   ,"updatedAt" DATE   ,"associatesApiKey" TEXT   ,"contentSelector" TEXT   ,"scanFreq" INTEGER   ,"user" TEXT NOT NULL  REFERENCES User(id),"site" TEXT NOT NULL  REFERENCES Site(id),PRIMARY KEY ("id"));

INSERT INTO "new_UserSite" ("id","createdAt","updatedAt","associatesApiKey","contentSelector","scanFreq","user","site") SELECT "id","createdAt","updatedAt","associatesApiKey","contentSelector","scanFreq","user","site" from "UserSite"

DROP TABLE "lift"."UserSite";

ALTER TABLE "lift"."new_UserSite" RENAME TO "UserSite";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190817081249..20190817081430
--- datamodel.dml
+++ datamodel.dml
@@ -26,9 +26,9 @@
   associatesApiKey String?
   contentSelector  String?
   user             User
   site             Site
-  scanFreq         Int
+  scanFreq         Int?
 }
 model Site {
   id        String     @default(cuid()) @id @unique
```

## Photon Usage

You can use a specific Photon built for this migration (20190817081430)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190817081430'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
